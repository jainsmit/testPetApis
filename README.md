# testPetApis
Tests to validate Pet APIs
* findPetById
* findPets
* addPet
* DeletePet

# Pre-requisite
* pip install requests
* pip install pytest
* python3 -m venv pytest-env

# To Run Test
* pytest -q tests/
